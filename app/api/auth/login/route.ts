import {NextResponse} from 'next/server';import {createSession,passwordMatches} from '@/lib/auth';export async function POST(request:Request){const {password}=await request.json().catch(()=>({password:''}));if(typeof password!=='string'||!passwordMatches(password))return NextResponse.json({error:'비밀번호가 올바르지 않아요.'},{status:401});await createSession();return NextResponse.json({ok:true})}
